# Pinned to the environment that produced the reported results.
#
# IMPORTANT: replace these pins with the actual output of
#   results/notebook08_final/<RUN_ID>/pip_freeze.txt
# after your locked run, and reconcile environment.yml against the same file.
# GATE-3 failed because M19's version table disagreed with both
# requirements.txt and environment.yml on every single entry. All three files
# and the manuscript must state the same versions.

numpy>=1.26,<3
pandas>=2.1,<3
scipy>=1.11
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.0
catboost>=1.2
shap>=0.44
imbalanced-learn>=0.12
ctgan>=0.10
matplotlib>=3.7
seaborn>=0.13
joblib>=1.3
openpyxl>=3.1
jupyter
nbconvert
